'use strict';

/**
 * host-business-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::host-business-setting.host-business-setting');
