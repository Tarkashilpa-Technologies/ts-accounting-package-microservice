'use strict';

/**
 * host-business-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::host-business-setting.host-business-setting');
